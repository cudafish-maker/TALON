# setup.py
# Backwards-compatible shim — all configuration is in pyproject.toml.
# This file exists only for editable installs on older pip versions:
#   pip install -e .

from setuptools import setup

setup()
