# T.A.L.O.N. — pinned dependencies
# Install: pip install -r requirements.txt
# For development: pip install -r requirements.txt -r requirements-dev.txt

# Reticulum Network Stack
rns>=1.1.3,<2.0
lxmf>=0.9.4,<1.0

# UI Framework
kivy>=2.3.0,<3.0
kivymd @ https://github.com/kivymd/KivyMD/archive/master.zip

# Tile-based map widget (kivy_garden.mapview)
mapview>=1.0.6

# Database (encrypted SQLite — requires libsqlcipher-dev on Linux)
sqlcipher3>=0.5.0,<1.0

# Cryptography
pynacl>=1.5.0,<2.0
argon2-cffi>=23.1.0

# Configuration
pyyaml>=6.0

# Serial port access (RNode LoRa hardware)
pyserial>=3.5,<4.0
